# Changelog

本插件版本演进记录。版本号遵循语义化版本（SemVer）。

## [0.6.0] - 2026-08-21

- 新增 Markdown 源码编辑模式：可在侧边栏预览/源码之间切换，支持 Cmd/Ctrl+S 与宿主保存按钮写回原 Markdown 文件。
- 复用 better-sidebar 的 `toolbar: 'host'` 协议，不新增第二个 Markdown viewer，避免与已有编辑插件冲突。

## [0.5.0] - 2026-08-20

重构与整改版本：拆模块、优化渲染、冻结 ref 契约、存储有界化、补测试与 CI。

### Changed

- **模块拆分**：`lib/client.js` 单体（1101 行）按职责拆为 `lib/src/{styles,store,parser,selection,panel,viewer,entry}.js`，由零依赖拼接脚本 `scripts/bundle.mjs` 合并为单文件（仍为无打包器、直发的 factory 格式）；`pnpm pack` 经 `prepack` 自动重新拼接，产物输出到 `dist/`。
- **渲染性能**：批注编辑器下沉为独立子组件（草稿本地 state，逐键输入不再重建整棵块树）；块级渲染预构建 `ref→notes` Map 与 `selectedIds` Set，消除 O(块×批注) 线性扫描；块/批注列表组件 `React.memo` + `useCallback` 稳定回调；面板拖拽/缩放改 ref 存几何 + `requestAnimationFrame` 节流、pointerup 一次性持久化；窗口 resize 节流；Escape 监听注册一次。
- **ref 契约版本化**：批注引用由 `i[:j]` 升级为 `v1:b:i:<sig>` / `v1:i:i:j:<sig>`（sig 为「前块+本块+后块」邻接上下文签名）；文件重生成后按「原文 + 上下文签名」重锚定，同名块不会静默错挂，失配保守标注「原文已变化」；旧格式引用兼容解码并盖版本戳。
- **解析器修正**：列表块补 `source`（符合"每块携带原文"契约）；列表项续行缩进不再被 trim（代码缩进保留）；`a|b\n---` 不再误判为表格。
- **存储有界化**：批注/面板偏好存储改为带 LRU 淘汰与 per-key 上限的工厂类（每次激活一实例，非模块级单例）；清空/发送后清空改为删除键；note 加 `v:1` schema 版本与正文长度上限；新增 JSON 导出按钮。
- **健壮性**：查看器外层包 ErrorBoundary（渲染异常降级为"预览出错 + 恢复"而非拖垮侧边栏）；设置读取/setPointerCapture/sessions.scope 等吞错处补 `console.debug` 日志；`inject` 显式声明 `betterSidebar/conversation/sessions`，发送路径改走 `props.ctx`（去掉模块级 ctx 全局）。
- **选区偏移解耦**：可批注内容隔离到专用 `.mdan-content` 子树，选区偏移只对内容子树计算，批注增删不再使存量选区偏移漂移。
- **面板几何集中**：默认宽高/最小尺寸/边距等魔数收敛为 `PANEL_DEFAULTS/PANEL_MIN/PANEL_MARGIN` + 单一 `clampPanel`。
- **小修**：批注 id 改 `crypto.randomUUID()`（带回退）；`new Highlight(...ranges)` 取代 bind.apply 写法；组件内 `var` 改 `const/let`；`DEFAULT_KIND` 常量、`kindLabel` 兜底从 `KIND_LABELS.suggest` 派生；空批注不再静默关闭编辑器（提示"批注内容为空"）；Escape 在宿主输入框内不误关浮层；图标按钮补 `aria-label`。

### Removed

- 列表块的"整块批注"入口（列表只做逐项批注）：旧行为会给整块引用写入 `source=undefined`，属潜在缺陷，现已移除。

### Added

- `scripts/bundle.mjs`、`test/unit.mjs`；`package.json` 补 `scripts/build/test/pack/clean`、`repository`、显式 `files` 白名单（不再发布 `lib/src`/`scripts`/`test`）。

## [0.4.2] - 2026-08-18

### Changed

- 「批注清单」毛玻璃透明度提高：面板背景 55% 不透明、标题栏 40% 不透明（原 72%/60%）。

## [0.4.1] - 2026-08-18

### Changed

- 「批注清单」悬浮面板改为毛玻璃半透明：背景 72% 不透明度 + 12px 背景模糊（backdrop-filter），保留明暗主题 token 并带实色回退；拖到对话区上方时能看到下方内容。

## [0.4.0] - 2026-08-18

### Added

- 批注清单支持逐条发送：每行「移除」左侧增加 `➤` 按钮，只把这一条写入对话框草稿。
- 批注清单支持勾选发送：每行左侧复选框；标题栏 `☑`/`☐` 全选或取消全选，`➤` 发送所选项（未勾选时禁用）。
- Escape 关闭浮层：先关编辑器，再关选区浮钮，再关清单。

### Changed

- 发送路径统一为 `sendNotes(list)`：底部「全部发送」、行内发送、发送所选项共用同一套报告格式；开启「发送后自动清空」时只清发出去的条目。
- 发送反馈缩短为「已发送 N 条」，完整说明放在悬停 title，避免挤换行底部栏。
- 清单标题改为「批注清单」，拖动说明改到 title。

## [0.3.5] - 2026-08-18

### Changed

- 底部栏发送按钮文案由「发送全部批注到对话框」改为「全部发送」。

## [0.3.4] - 2026-08-18

### Changed

- 底部栏手柄改为抽屉惯例：展开显示 `﹀`（点一下往下收），收起只显示 `︿`（点一下往上开），不再使用 `✍`，收起状态也不再显示批注条数。

## [0.3.3] - 2026-08-18

### Added

- 底部批注操作栏可手动收起/展开：栏顶增加可点击手柄。展开时显示 `﹀`，点一下收起操作按钮、只留一条约 22px 的手柄；收起时显示 `︿`，点一下展开。默认展开；开合状态按「会话 + 文件」记忆，与批注清单开合一致。收起后「发送全部批注到对话框」不可见，避免误发。

## [0.3.2] - 2026-08-16

### Changed

- 「发送后自动清空批注」默认值由关闭改为**开启**：发送即交付，写入对话框草稿后自动清空本文件批注；修复多轮批注工作流中上一轮批注被一并带上发送的问题。需要跨轮累积批注时可在设置页关闭该开关。设置项描述与发送反馈文案同步更新。

## [0.3.1] - 2026-08-16

### Fixed

- 批注入口重复冲突：选中文字后（鼠标仍停留在块内、块保持 hover），块级「＋批注」按钮与「＋批注选区」浮按钮同屏并存、易误点。现在文字选区 pending 或批注编辑器打开期间，块级 hover 按钮自动隐藏，同一时刻只保留一个明确入口。

### Changed

- 选区批注的批注条目增加琥珀色左边框（与"已批注块"视觉语言统一），配合引用行，与块级批注一眼区分。

## [0.3.0] - 2026-08-16

### Added

- 选中任意文字批注：在预览中拖选不限字数的文字（需落在同一个段落/列表项内），浮出「＋ 批注选区」按钮，编辑器顶部显示选中引用条。
- 被批注的选中文字持续琥珀色高亮：基于 CSS Custom Highlight API（纯 Range 注册、不改 DOM，与 React 渲染零冲突；不支持该 API 的引擎自动降级为仅显示批注条目）。
- 批注引用精确到选中字句：块下批注条目、批注清单、发送到对话框的报告 Excerpt 均引用选中文字而非整块原文。
- 文件重新生成后的选区重定位：按「块匹配 → 偏移校验 → 全文重找」三级策略恢复高亮，失配时沿用"原文已变化"标注。

## [0.2.2] - 2026-08-16

### Changed

- 「批注清单」悬浮面板拖动范围从侧边栏预览区放宽到整个视口，可拖出侧边栏覆盖对话区使用。
- 面板缩放上限改为视口边界（原先受预览区宽度约 384px 限制，拖出后无法拉宽）。
- 面板 z-index 30 → 60，避免被宿主浮层局部遮挡。

### Fixed

- 浏览器窗口缩小后，落在视口外的悬浮面板自动拉回可视区（新增 window resize 监听，位置持久保存）。

## [0.2.1] - 2026-08-16

### Fixed

- 底部批注汇总栏在短文档（内容不足一屏）时悬停在内容中间、不贴底的问题：根容器改为纵向 flex + 内层 `.mdan-scroll` 滚动区结构，汇总栏移出滚动区、永远贴底；长文档滚动时内容不再从汇总栏下方穿过。

## [0.2.0] - 2026-08-16

首个公开版本。

### Added

- 在 dsh-better-sidebar 的 Markdown 预览中接管 `.md` 文件，解析为可批注块：标题、段落、列表项（逐项）、表格、代码块、引用、水平线。
- 块级批注：悬停出现「＋批注」，支持增/改/删、一处多条；类型标签（必须改 / 建议改 / 疑问）。
- 批注清单悬浮面板：可拖动、可调整大小、单条移除、一键清空、点击定位到原文。
- 一键发送全部批注到对话框草稿：按类型分组的结构化修改指令（文件路径 + 行号 + 原文引用 + 批注），可配置发送前缀与发送后自动清空。
- 文件重新生成后按「块序号 + 原文」重匹配批注，失配项标注"原文已变化"。

## 0.1.x

早期内部迭代版本（未对外发布）。
